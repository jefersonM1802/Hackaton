# --- CORE DEL SERVIDOR (Flask) ---
Flask>=3.0.0
Werkzeug>=3.0.1
python-dotenv>=1.0.0

# --- BASE DE DATOS (ORM) ---
# Aquí estaba el error grave. Usamos >= para traer la versión compatible con Py3.13
Flask-SQLAlchemy>=3.1.1
SQLAlchemy>=2.0.36

# --- AUTENTICACIÓN Y SEGURIDAD ---
Flask-Login>=0.6.3
email-validator>=2.1.0

# --- INTELIGENCIA ARTIFICIAL ---
google-generativeai>=0.4.0
Pillow>=11.0.0               # Actualizado para evitar error de compilación

# --- CIENCIA DE DATOS Y FINANZAS ---
# Quitamos los == para que pip busque los binarios compatibles con Mac M1/M2/M3
pandas>=2.2.0
openpyxl>=3.1.2
numpy>=1.26.0

# --- REPORTES Y PDFs ---
WeasyPrint>=60.1
# OJO: WeasyPrint en Mac requiere instalar librerías externas.
# Si te falla, comenta la línea de arriba y usa solo Excel por ahora.

# --- UTILIDADES ---
requests>=2.31.0        # Para peticiones HTTP externas (si fueran necesarias)