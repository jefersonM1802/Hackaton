# --- CORE DEL SERVIDOR (Flask) ---
Flask==3.0.0
Werkzeug==3.0.1
python-dotenv==1.0.0        # Para gestionar claves secretas (.env)

# --- BASE DE DATOS (ORM) ---
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.23

# --- AUTENTICACIÓN Y SEGURIDAD ---
Flask-Login==0.6.3
email-validator==2.1.0.post1 # Para validar correos reales en registro

# --- INTELIGENCIA ARTIFICIAL (Gemini & Imágenes) ---
google-generativeai>=0.4.0   # SDK oficial de Gemini
Pillow==10.1.0               # Procesamiento de imágenes (Recibos/Facturas)

# --- CIENCIA DE DATOS Y FINANZAS (Pandas) ---
pandas>=2.1.0                # Cálculos financieros y proyecciones
openpyxl==3.1.2              # Motor para leer/escribir Excel
numpy>=1.26.0                # Necesario para cálculos matemáticos de Pandas

# --- REPORTES Y PDFs ---
WeasyPrint==60.1             # Para convertir HTML a PDF (Reportes bonitos)
# Nota para Windows: WeasyPrint a veces requiere instalar GTK3 por separado.
# Si da problemas, comenten la línea de arriba y usen solo Excel.

# --- UTILIDADES ---
requests==2.31.0             # Para peticiones HTTP externas (si fueran necesarias)